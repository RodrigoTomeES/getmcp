{
  "name": "getmcp",
  "private": true,
  "description": "Universal installer and configuration tool for MCP servers across all AI applications",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/RodrigoTomeES/getmcp.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @getmcp/core && npm run build -w @getmcp/generators && npm run build -w @getmcp/registry && npm run build -w @getmcp/cli && npm run build -w @getmcp/web",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "clean": "rimraf packages/*/dist packages/*/tsconfig.tsbuildinfo packages/web/.next"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "@types/node": "^24.0.0"
  }
}
